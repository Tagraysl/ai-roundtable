# 代码结构与迭代约定

## 0.11.6 发布边界

`scripts/release.ps1` 是对外构建入口：固定 Electron 版本，验证上游 SHA256，每次使用新目录，只复制明确列出的源码及文档。不要使用开发安装目录直接压缩发布。源码仓库不包含开发 QA 数据和历史修改脚本；GitHub Actions 执行核心测试、构建和文件检查。

`conversation-context.cjs` 负责上下文范围，`handoff.cjs` 负责接续资料，`model-catalog.cjs` 负责模型列表。使用方式与历史选取分别保存；界面不应将两者合并。

工作流依赖恢复由 `resume-selection.cjs` 与 runner 共同控制：仅重试所选节点及就绪后继，复用成功节点，其他失败节点构成新的停止边界。普通讨论和工作流的调度语义不同。

当前只开放 API、Codex、网页手动接力。通用终端及自动网页代码暂保留兼容，但主进程拒绝使用，不能在宣传中当作可用功能。英文界面仍有后端错误与系统对话框的覆盖缺口。下面保留早期模块划分及迭代约定，功能状态以本节和 README 为准。

## 本轮审查与改动

检查了主进程 IPC、API/Codex/终端适配器、工作流调度、聊天状态、数据存储和 UI 样式加载方式。当前代码存在重复的视觉覆盖规则、动态插入界面的顺序依赖，以及体积较大的 main.cjs/workflow.js。此次不宣称完成全量重写或证明所有行为无缺陷。

- `request-recovery.cjs`：集中定义网络异常分类、HTTP 错误、Retry-After、退避、停止和状态通知。API 流式请求与工具模式复用，避免各个服务商分别实现重试。
- `adapters/sse.cjs`：从 API 业务适配器分离事件流解析，保留原导出兼容性。
- `ui/controls.css`：统一所有原生 select 的外观、焦点、禁用和选项颜色，涵盖动态创建的设置及工作流选择框。保留原生表单与键盘行为。展开弹层的部分绘制仍由 Electron/Windows 决定。
- 现有 JSON 数据格式、IPC 名称、成员适配协议不变，不迁移用户数据。

## 恢复边界

API 在收到可见回答之前，遇到连接关闭/重置、DNS 或临时服务异常、HTTP 408/429/500/502/503/504，最多重试两次。通常等待 1 秒、3 秒；服务商 Retry-After 最长采纳 30 秒。整个请求共享原来的超时预算，用户停止会取消等待。

流式输出已有文字时不自动重放，保留未完成文本。API 工具模式只重试当前模型请求，不重跑已经完成的搜索工具。重试不保证服务端未计费，也不能保证服务器上先前的生成已取消。401/403、格式/模型等配置错误不重复提交。Codex、本地执行、人工确认不采用此自动重放策略。

工作流仍提供节点详情中的“从此节点重跑”，沿用现有上游复用与任务/附件/技能变更校验。

## 后续扩展应遵守

1. 服务商 URL/名称等声明放入 provider-catalog.json。新增通信协议实现独立 adapter，不按成员显示名称选择驱动。
2. adapter 接收统一的 member/prompt/signal/onText/onStatus，不能直接操作窗口或写聊天记录。
3. workflow.cjs 负责依赖与执行状态；UI 只展示状态。重试策略属于请求层，不能自动重放有本地副作用的执行节点。
4. 持久化功能通过主进程 store/IPC 实现；用户文本通过 textContent 展示。删除需确认，历史快照不随模板变化。
5. 新控件优先使用公共控件样式和颜色变量，避免继续在每个功能文件重复覆盖 select。
6. 后续分批将 main.cjs 的 IPC 按领域拆为注册模块，将 workflow.js 拆为画布、检查器、目录、运行视图；先保留接口并添加覆盖，再迁移调用。不要同时改数据模型与界面框架。

## 验证

Node 回归测试覆盖 API 两种协议、附件、工作流依赖/重跑/确认、技能权限、存储迁移、导出、停止/补充等；恢复测试覆盖退避、取消、不重放部分输出和最大次数。实际服务商、代理、浏览器与本地账号环境不能由本地模拟测试完全代表。
