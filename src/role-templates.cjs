const {randomUUID}=require('node:crypto');
const builtins=[['code','编写代码','根据用户目标与上游方案实现代码，按相对文件路径逐一给出完整可运行的代码块、启动方式与验证步骤。不要仅分析或提出建议；不要声称已在本地保存或运行。'],['local_verify','本地验证','优先使用上游提供的代码，在指定目录中保存必要文件并实际运行可用测试。不要另写一套实现替代上游代码；缺少代码时明确报告缺失。报告执行命令、实际结果和未验证项，必要修复须说明原因及修改。'],['analysis','分析','分析问题、已有证据与关键假设，给出独立判断，明确不确定性。'],['plan','规划','提出总体框架与可操作步骤，说明依赖、优先级和完成标准。'],['review','检查','审查上游方案的逻辑、事实与遗漏，逐项指出问题及修改建议。'],['verify','验收','对照原目标和完成标准检查结果，说明通过项、未完成项与可核查证据。'],['summary','汇总','整合上游意见，区分共识、分歧和证据缺口，提出下一步建议。']].map(([id,name,content])=>({id:'builtin_'+id,name,content,builtin:true}));
function roles(store){let custom=store.read('role-templates',[]);return {list:()=>structuredClone([...builtins,...custom]),save(raw){if(typeof raw.name!=='string'||!raw.name.trim()||raw.name.length>60||typeof raw.content!=='string'||!raw.content.trim()||raw.content.length>4000)throw Error('模板需要名称（最多60字）和职责正文（最多4000字）。');const old=custom.find(x=>x.id===raw.id);if(!old&&custom.length>=100)throw Error('最多100个自定义职责模板。');const item={id:old?.id||randomUUID(),name:raw.name.trim(),content:raw.content};custom=old?custom.map(x=>x.id===old.id?item:x):[...custom,item];store.write('role-templates',custom);return item;},remove(id){if(!custom.some(x=>x.id===id))throw Error('只能删除自定义模板。');custom=custom.filter(x=>x.id!==id);store.write('role-templates',custom);store.write('role-templates',custom);}};}
module.exports={roles,builtins};
