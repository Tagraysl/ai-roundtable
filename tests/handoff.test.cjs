const test=require('node:test'),assert=require('node:assert/strict');const {preview,candidates}=require('../src/handoff.cjs');
test('handoff only exposes own run attachments and includes text materials in workflow context',()=>{const room={id:'r',workflowId:'g',materials:[{name:'note',text:'KEEP NOTE'},{attachmentId:'owned',name:'old'}],messages:[{name:'AI',role:'assistant',text:'KEEP ANSWER',status:'complete'},{text:'FAILED',status:'error'}]};const runs=[{roomId:'other',attachments:[{attachmentId:'secret'}]},{roomId:'r',attachments:[{attachmentId:'owned'},{attachmentId:'new',name:'file'}]}];assert.deepEqual(candidates(room,runs).map(a=>a.attachmentId),['new']);const p=preview(room,null,runs);assert.match(p.text,/KEEP NOTE/);assert.match(p.text,/KEEP ANSWER/);assert.doesNotMatch(p.text,/FAILED/);room.workflowId='';assert.match(preview(room,{name:'solo'},runs).text,/KEEP ANSWER/);});
